{
  "name": "automerge-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "workers:dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "@automerge/automerge": "3.5.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "7.3.0",
    "svelte": "5.57.1",
    "vite": "8.3.0",
    "wrangler": "4.136.2",
    "vite-plugin-wasm": "3.6.0",
    "vitest": "5.0.1"
  }
}
